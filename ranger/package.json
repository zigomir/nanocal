{
  "name": "nanocal-ranger",
  "version": "0.2.0",
  "main": "dist/ranger.cjs.js",
  "module": "dist/ranger.esm.js",
  "browser": "dist/ranger.min.iife.js",
  "repository": "https://github.com/zigomir/nanocal/tree/master/ranger",
  "license": "MIT",
  "homepage": "https://nanocal.zigomir.com",
  "bugs": "https://github.com/zigomir/nanocal/issues",
  "description": "Minimal, AirBnb inspired, range picker",
  "dependencies": {
    "cntdys": "^0.2.5"
  },
  "scripts": {
    "build": "../node_modules/.bin/tsc && npm run build:cjs && npm run build:module && npm run build:iife && ../node_modules/.bin/bundlesize",
    "build:cjs": "../node_modules/.bin/rollup src/main.js -c ../rollup.config.js -f cjs -o dist/nanocal-ranger.cjs.js",
    "build:module": "../node_modules/.bin/rollup src/main.js -c ../rollup.config.js -f es -o dist/nanocal-ranger.esm.js",
    "build:iife": "../node_modules/.bin/rollup src/main.js -c ../rollup.config.js -f iife -o dist/nanocal-ranger.min.iife.js",
    "test": "npm run --prefix .. test",
    "prepublishOnly": "npm run build && npm run test"
  },
  "bundlesize": [
    {
      "path": "./dist/nanocal-ranger.min.iife.js",
      "maxSize": "5.5 kB"
    }
  ]
}
